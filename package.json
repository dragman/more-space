{
  "name": "more-space-ui",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "bash -lc 'source ~/.nvm/nvm.sh && nvm use >/dev/null && npx tsc --noEmit'",
    "wasm": "wasm-pack build --target web --out-dir pkg --out-name more_space",
    "build:local": "npm run wasm && npm run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@babylonjs/core": "^8.44.1",
    "@babylonjs/inspector": "^8.44.1",
    "@babylonjs/materials": "^8.44.1"
  }
}
